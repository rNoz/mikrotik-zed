# AGENTS.md — Zed Extension: MikroTik RouterOS Script (RSC)

## Project Goal

Build a Zed editor extension that adds support for the MikroTik RouterOS scripting language (`.rsc` files), delivered in two phases:

1. **Phase 1 (MVP):** Tree-sitter based syntax highlighting, no Rust code required.
2. **Phase 2:** A Rust-based language server providing autocompletion and hover for all RouterOS menus. Pure Rust — no Node.js dependency.

This document is the operating guide for any agent (human or AI) contributing to this repository.

## Language Convention

All code, comments, documentation, commit messages, and PR descriptions **must be in English**. The RouterOS documentation source (`llms-full.txt`) is in English. The Zed extension ecosystem is English-only.

---

## Current Status

| Milestone | Status | Notes |
|-----------|--------|-------|
| Grammar defined (`grammar.js`) | Done | 244 lines, covers menus, globals, control flow, variables, arrays |
| Grammar generated (`src/parser.c`) | Done | `tree-sitter generate` completed |
| Query files (`highlights.scm`, etc.) | Done | 5 query files in `languages/rsc/` |
| Language config (`config.toml`) | Done | RSC, `.rsc` suffix, brackets, word chars |
| Extraction script (`extract_commands.py`) | Done | Parses `llms-full.txt` → `commands.toml` |
| `data/commands.toml` generated | Done | 10,557 lines, 163 menus covering 4 target menus |
| Grammar repo published | Done | https://github.com/rNoz/mikrotik-rsc-grammar, pinned in `extension.toml` |
| Phase 1 tested in Zed | **Not done** | Needs "Install Dev Extension" validation |
| Phase 1 published to zed-industries/extensions | **Not done** | PR not submitted |
| Phase 2 language server | Done | Native Rust binary at `lsp/`, WASM extension at `extension/` |
| Node.js dependency removed | Done | `ls.mjs` deleted; LSP now pure Rust (`rsc-ls` binary) |

---

## Repository Layout

```
mikrotik-zed/                          # Monorepo: extension + grammar
├── AGENTS.md                          # This file — operating guide for agents
├── LICENSE                            # Apache-2.0
├── extension.toml                     # Zed extension manifest
├── llms.txt                           # RouterOS doc index (608 lines)
├── llms-full.txt                      # Full RouterOS 7.22 docs (108K lines, truth source)
│
├── grammars/rsc/                      # Tree-sitter grammar (in-tree)
│   ├── grammar.js                     # Grammar rules (244 lines)
│   ├── Cargo.toml                     # Rust crate for WASM build
│   ├── package.json                   # Node/npm metadata
│   ├── tree-sitter.json               # Tree-sitter metadata
│   ├── src/                           # Generated by `tree-sitter generate`
│   │   ├── parser.c
│   │   ├── grammar.json
│   │   └── node-types.json
│   └── test/
│       ├── simple.rsc                 # Minimal test case
│       └── example.rsc                # Rich test case (26 lines)
│
├── languages/rsc/                     # Zed language queries
│   ├── config.toml                    # Language config (name, suffixes, brackets)
│   ├── highlights.scm                 # Syntax highlighting (97 lines)
│   ├── brackets.scm                   # Bracket matching (5 lines)
│   ├── indents.scm                    # Indentation rules (3 lines)
│   ├── outline.scm                    # Outline/symbol view (7 lines)
│   └── injections.scm                 # Code injections (empty — RSC has none)
│
├── data/
│   └── commands.toml                  # Generated command table (10,557 lines)
│
├── scripts/
│   ├── extract_commands.py            # Extracts commands.toml from llms-full.txt
│   └── debug_extract.py              # Debug helper
│
├── Cargo.toml                         # Root package (WASM cdylib) + workspace
├── src/
│   └── lib.rs                         # Zed extension trait impl (downloads binary)
│
├── lsp/                               # Phase 2 — LSP binary (native Rust, workspace member)
│   ├── Cargo.toml
│   └── src/
│       ├── main.rs                    # LSP wire protocol + dispatch
│       ├── menus.rs                   # TOML loading + menu indices
│       ├── completion.rs              # Completion logic
│       └── hover.rs                   # Hover logic
│
└── .agents/skills/                    # OpenCode skills (auto-discovered)
    ├── routeros-reference.md          # How to look up RouterOS commands
    ├── zed-extension-dev.md           # Zed extension development patterns
    ├── tree-sitter-grammar.md         # Grammar maintenance guide
    ├── commands-extraction.md         # Regenerating commands.toml
    ├── language-server.md             # Phase 2 LS implementation guide
    └── development-workflow.md        # General development workflow
```

### Key Facts

- **Extension ID:** `mikrotik-rsc` (not `routeros-script`)
- **Extension Name:** `MikroTik RouterOS Script`
- **Language name:** `RSC`
- **Grammar:** In-tree at `grammars/rsc/` (separate repo `fravic/mikrotik-rsc-grammar` exists but grammar source lives here)
- **Truth source for commands:** `llms-full.txt` (auto-generated from RouterOS itself)
- **Target RouterOS version:** 7.22+

---

## Phase 1 — Grammar and Highlighting (No Rust)

### Steps

1. Grammar is defined in `grammars/rsc/grammar.js`. To modify:
   ```bash
   cd grammars/rsc
   # Edit grammar.js
   npx tree-sitter generate
   npx tree-sitter test
   ```
2. Query files are in `languages/rsc/`. Edit order of priority:
   - `highlights.scm` (highest — syntax coloring)
   - `brackets.scm` (bracket matching)
   - `indents.scm` (auto-indent)
   - `outline.scm` (symbol view)
   - `injections.scm` (placeholder, no injections yet)
3. Test locally: Zed → "Install Dev Extension" → point to `mikrotik-zed/`.
4. If the build fails with a wasm32-wasip1 linker error mentioning `-mmacosx-version-min`, check that any macOS-only rustflags in `~/.cargo/config.toml` are scoped under `[target.*-apple-darwin]` instead of the global `[build]` section. A global `[build] rustflags` value is merged into every target, including the WASM linker invocation.
5. Publish via PR to `zed-industries/extensions` (submodule + entry in `extensions.toml` + `pnpm sort-extensions`).

### Phase 1 Exit Checklist

- [ ] `.rsc` files highlight correctly in Zed.
- [ ] Bracket matching works for `[...]`, `{...}`, `(...)`.
- [ ] Outline view reflects the menu hierarchy.
- [ ] `extension.toml` grammar `rev` points to a real commit (not `0000...`).
- [ ] Valid OSS license in repo root (Apache-2.0 — already present).
- [ ] PR merged into zed-industries/extensions.

---

## Phase 2 — Language Server (Pure Rust binary)

### Architecture

```
Zed  →  WASM extension (src/lib.rs)  →  launches  →  rsc-ls (native Rust binary)
```

- `src/lib.rs` — WASM cdylib crate (root package).  Implements `zed_extension_api::Extension`.
  On `language_server_command`, searches PATH for `rsc-ls` binary and launches it.
- `lsp/` — native Rust binary (workspace member).  Embeds `commands.toml` at compile time via `include_str!()`.
  Communicates over stdio using JSON-RPC 2.0 (LSP protocol).

### Coverage (Full)

All root menus from `llms-full.txt` are extracted: `/interface`, `/ip`, `/ipv6`,
`/routing`, `/queue`, `/system`, `/tool`, `/user`.  Total: 685 menus.

### Data Source for the Command Table

The extraction pipeline is already built:

1. **Truth source:** `llms-full.txt` (108K lines, auto-generated from RouterOS 7.22).
2. **Extraction script:** `scripts/extract_commands.py` — parses ArgTable XML from `llms-full.txt`.
3. **Output:** `data/commands.toml` — consumed by the language server.
4. **Regeneration command:**
   ```bash
   python3 scripts/extract_commands.py
   ```
5. **Regenerate when:** New RouterOS v7 major release ships, or extraction logic is improved.
6. **Spot-check against:** `manual.mikrotik.com/docs/cli-reference/` or `/export` + `[Tab]` on a real router.

### Implementation Steps

1. Build the LSP binary:
   ```bash
   cargo build -p rsc-ls --release
   ```
2. The binary is now at `target/release/rsc-ls`.  Add it to PATH before launching Zed.
3. Build the WASM extension:
   ```bash
   cargo build -p mikrotik-zed --target wasm32-wasip1 --release
   cp target/wasm32-wasip1/release/mikrotik_zed.wasm extension.wasm
   ```
4. Test with "Install Dev Extension"; inspect logs via `zed --foreground` or the `zed: open log` action.
5. If the WASM build fails with a linker error about `-mmacosx-version-min` or other macOS flags, scope those rustflags to `[target.*-apple-darwin]` in `~/.cargo/config.toml`; global `[build] rustflags` leak into the wasm32-wasip1 linker.

### Phase 2 Exit Checklist

- [ ] Autocompletion works for all 8 root menus (685 total menus).
- [ ] Hover shows documentation for menu paths and properties.
- [ ] `commands.toml` is versioned against a specific RouterOS build.
- [ ] Documented process for regenerating the table from `llms-full.txt`.
- [ ] `rsc-ls` binary compiles and runs on all target platforms (macOS aarch64/x86_64, Linux aarch64/x86_64).

---

## Rules and Constraints

- Never clone or build `zed-industries/zed`. Only depend on `zed_extension_api`.
- Do not bundle the language server binary inside the extension repo: download or build it at install time.
- The extension `id` and `name` must not contain the words "zed" or "extension".
- A valid OSS license is mandatory in the repo root (Apache-2.0, MIT, GPLv3, LGPLv3, BSD, Unlicense, or zlib).
- All Rust code must compile to `wasm32-wasip1` (or whichever target `zed_extension_api` currently requires).
- Keep `grammar.js` and `commands.toml` as separate, independently versioned sources of truth.
- All code, comments, docs, and commit messages in English.
- Never use `std::env::var` or `cfg` directives in extension Rust code — use `zed_extension_api` methods.

## Reference Sources

| Resource | URL |
|----------|-----|
| Zed extension development | https://zed.dev/docs/extensions/developing-extensions |
| Zed language extension docs | https://zed.dev/docs/extensions/languages |
| `zed_extension_api` crate | https://docs.rs/crate/zed_extension_api/latest |
| RouterOS CLI Reference | https://manual.mikrotik.com/docs/cli-reference/ |
| Full docs (truth source) | https://manual.mikrotik.com/llms-full.txt |
| Doc index | https://manual.mikrotik.com/llms.txt |
| Central extensions registry | https://github.com/zed-industries/extensions |
| Grammar repo | https://github.com/fravic/mikrotik-rsc-grammar |
